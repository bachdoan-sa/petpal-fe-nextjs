import { ServiceType } from "@/src/schemaValidations/service.schema";

type Service = ServiceType;
export default function SideListService({list}:{list: Service[]}) {

    return (
        <>
        </>
    )
}